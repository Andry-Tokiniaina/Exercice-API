openapi : "3.0.3"
info:
  title : ping
  version : 1.0.0
servers:
  url: https://localhost:8000
paths:
  /ping:
  get:
    responses:
      200:
        content:
          text/plain:
          schema:
            type: string
            example: pong